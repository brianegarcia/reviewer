/**
 * Step 10 – REMOVED
 *
 * practice-fusion/phase2/10-diagnoses.ts was deleted in commit "fix: general fixes".
 *
 * The patient diagnosis timeline is no longer populated as a separate step.
 * Diagnoses are now referenced via the diagnosisLookup built in Phase 3 and
 * used by step 16 (encounter-diagnoses → impressions_and_plan).
 *
 * This file exists only as a tombstone so the step numbering in the folder
 * stays consistent with the original scripts.
 */

// No export — this step is not registered in the pipeline.
